#!/bin/bash
#
# Use pandoc to generate HTML from Markdown

mkdir -p _site

for file in *.md
do
    pandoc \
        --template=_layout/swc \
        -f markdown -t html5 \
        -o _site/${file/\.md/\.html} ${file}
done
